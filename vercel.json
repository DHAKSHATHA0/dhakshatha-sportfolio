{
  "buildCommand": "npm run build && node scripts/generate-dist-index.cjs",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
